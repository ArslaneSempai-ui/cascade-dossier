{
  "name": "cascade-dossier",
  "version": "0.1.0",
  "description": "Is the whole compliance chain measured, sealed, fresh and yours: one dossier a reviewer verifies without us. Nothing of yours goes up.",
  "type": "module",
  "license": "PolyForm-Noncommercial-1.0.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "tsc --noEmit && node src/readme.ts --check && node src/licences.ts --check && node --test src/*.test.ts src/*.test.mjs",
    "dossier": "node src/cli.ts",
    "figures": "node src/readme.ts",
    "measure": "node src/measure.ts",
    "sceller": "node src/sceller.ts",
    "verify": "node src/verifier-rapport.mjs",
    "licences": "node src/licences.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.0"
  }
}
